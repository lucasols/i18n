{
  "name": "@ls-stack/i18n",
  "description": "Super simple browser i18n library with React support",
  "version": "0.10.2",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "tsc": "tsc -p tsconfig.prod.json",
    "build:no-test": "tsdown",
    "build": "pnpm tsc && pnpm test && pnpm build:no-test",
    "test-cli:check": "pnpm build:no-test && node dist/cli.mjs --default en --config-dir ../cli-test/config --src-dir ../cli-test/src",
    "test-cli:fix": "pnpm test-cli:check --fix",
    "pre-publish": "../scripts/check-if-is-sync.sh && pnpm build"
  },
  "files": [
    "dist"
  ],
  "bin": {
    "ls-stack-i18n": "./dist/cli.mjs"
  },
  "repository": "https://github.com/lucasols/i18n",
  "author": "Lucas Santos",
  "type": "module",
  "main": "./dist/main.mjs",
  "types": "./dist/main.d.mts",
  "exports": {
    ".": {
      "import": "./dist/main.mjs",
      "types": "./dist/main.d.mts"
    }
  },
  "sideEffects": false,
  "engines": {
    "node": ">=21.5.0"
  },
  "dependencies": {
    "@ls-stack/utils": "^3.68.0",
    "type-flag": "^3.0.0",
    "ai": "^6.0.69",
    "zod": "^3.24.0",
    "readdirp": "^5.0.0",
    "runcheck": "^1.15.0",
    "@ai-sdk/google": "^3.0.20",
    "@ai-sdk/openai": "^3.0.25",
    "@ai-sdk/provider": "^3.0.7"
  },
  "devDependencies": {
    "@ls-stack/i18n-core": "workspace:*",
    "@testing-library/react": "^16.0.0",
    "typescript": "^5.8.3"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "@types/react": "^18.0.0",
    "typescript": "^5.8.3"
  },
  "peerDependenciesMeta": {
    "react": {
      "optional": true
    },
    "@types/react": {
      "optional": true
    }
  }
}
